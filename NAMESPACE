export( RL4,
        print.rl4,
        runs.pvalue,
        pruns.exact,
        sequences,
        williams )

# import functions from stats
importFrom(stats, runif, na.omit, median, pnorm)

# Define S3 methods
S3method(print, rl4)
